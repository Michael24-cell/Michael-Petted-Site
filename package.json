{
  "name": "michael-petted-site",
  "private": true,
  "scripts": {
    "dev": "npx --yes live-server --host=localhost --port=3000 --open=/index.html",
    "dev:alt": "npx --yes live-server --host=localhost --port=3001 --open=/index.html"
  },
  "dependencies": {
    "resend": "^6.18.1"
  }
}
